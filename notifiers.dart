import 'package:flutter/material.dart';

// valueNotiifer: hold the data
// ValueNotifierBuilder: Listen to the data (don't need the setstate)
ValueNotifier <int> selectedPageNotifier = ValueNotifier(0);

ValueNotifier <bool> isDarkModNotifier = ValueNotifier(true);


